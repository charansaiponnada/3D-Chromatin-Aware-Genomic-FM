# Laptop (Phases 0-2, CPU only):  pip install -e .
# GPU box (Linux): plain `pip install -e .` already pulls a CUDA build of torch.
#   Verified: torch 2.14.0+cu130 on 2x L40S, driver CUDA 13.0.
#   Older driver? install torch first from a matching index, e.g.
#   pip install torch --index-url https://download.pytorch.org/whl/cu126
#   (cu121 stops at torch 2.5.1, below the floor below -- do not use it.)

# --- core ---
numpy>=1.26,<3
pandas>=2.1
pyyaml>=6.0
scipy>=1.11
tqdm>=4.66

# --- genomics ---
cooler>=0.9.3          # .cool / .mcool contact matrices
pyfaidx>=0.8.1         # random access into hg38 without loading 3.1 GB
h5py>=3.10
requests>=2.31

# --- model ---
# 2.6 is the floor for Python 3.13 wheels. On 3.11 anything >=2.2 works, but
# pinning the higher floor keeps one requirements file valid on both.
torch>=2.6
